# 前后端联动开发问题记录

> 目的：记录前端（`D:\code\memory\agent-memory-frontend`）与后端（`D:\code\memory\claudecode\memProject`）联动开发过程中发现的问题、讨论结论与待办。
>
> 约定：每个问题标注状态 —— `[ ]` 待讨论 / `[~]` 讨论中 / `[x]` 已解决（附结论与日期）。

---

## 一、已发现的前后端不一致问题

### 1. `/memory/list` HTTP 方法不匹配（会导致 405）

- **前端**：`src/api/modules/memory.ts:102` 用 `method: 'GET'` 调 `/api/v1/memory/list`
- **后端**：`app/api/v1/memory.py:744` 是 `@router.post("/list")`
- **影响**：前端列表请求会 405，功能不可用
- **落地影响（多层记忆管理全失效）**：`Memory/index.tsx` 的 `loadMemories` 依赖 `listMemories`，用户级/会话级/任务级/智能体级四个层级列表全部 405 加载失败，仅顶部 `getMemoryStats`（GET /stats）卡片可用
- **状态**：`[ ]`

### 2. `/memory/write` 响应结构过时

- **前端**：类型 `MemoryWriteResult = { mode, results[] }`（`src/api/types.ts:297`），`MemoryWriteItem`（`id/memory/event`）
- **后端**：已改为只落 L0，返回 `{ accepted, session_id, l0_count, record_ids }`
- **影响**：前端类型与实际响应不符，写入结果无法正确展示
- **落地影响（导入界面不可用）**：`Ingestion/index.tsx:150-153` 执行 `result.results.filter(...)`，但后端未返回 `results`，`result.results` 为 `undefined`，`.filter` 抛 TypeError → 每条写入都进 catch，导入判为 failed/partial。且「生成 N 条记忆」统计依赖旧的同步返回 `results`，在异步抽取架构下已无法从 write 响应即时得知
- **状态**：`[ ]`

### 3. `/memory/write` 超时与注释过时

- **前端**：`memory.ts:40` 注释「耗时 5-15 秒」+ `timeout: 60000`
- **后端**：只落 L0，毫秒级返回
- **影响**：超时冗余（无功能 bug），但注释误导
- **状态**：`[ ]`

### 4. `/memory/delete` 前端存在 500 降级 workaround

- **前端**：`memory.ts:155-183` 捕获 500 后降级调 `/memory/update` 置 `status=deleted`；注释称「deployed 单删端点返回 500」
- **后端**：`/memory/delete` 软删除逻辑（`memory.py:721` 调 `memory_store.soft_delete`）
- **影响**：需确认后端 delete 是否真有 500 bug，还是 workaround 已失效
- **状态**：`[ ]`

### 5. 前端未接入 `/memory/profile`（L3 画像）—— 命名混淆澄清

- **前端**：`/profile` 路由 → `Profile/index.tsx` 是「**个人中心**」（展示/编辑登录账号资料：姓名/邮箱/手机号/部门，数据来自 `authStore.user` mock 登录），**与记忆画像无关**
- **后端**：`/memory/profile` 是 L3 记忆画像（消费 L2 场景块，返回 persona 自由文本 `{persona, scene_id, changed_scenes}`）
- **澄清（2026-08-19）**：两者 URL 都带 "profile" 但**毫无关系** —— 前端是「账号个人中心」，后端是「L3 记忆画像」。真正的问题是前端**没有**「L3 记忆画像」展示页面（不是"没接"，是"压根没有这个页面"）
- **影响**：L3 画像（核心产出）前端无展示入口
- **待定**：是否新增「记忆画像」展示页（调 `/memory/profile` 展示 persona），还是画像仅供 Proxy/外部 agent 用、管理面板不展示
- **状态**：`[~]`

### 6. 控制台登录：前端 mock，后端无接口

- **前端**：`Login/index.tsx` + `authStore.ts` 用硬编码 `MOCK_USERS`（admin/admin123、operator/operator123）纯前端校验，token 为前端伪造 `mock-token-xxx`
- **后端**：无 `/login`、`/register`、`/auth/*` 任何登录端点；后端鉴权是 `X-API-Key`（智能体接入鉴权），非用户登录
- **影响**：登录信息不发给后端、不影响后端行为；仅控制前端路由守卫 `RequireAuth`
- **结论**（初步）：当前登录「形同虚设」。需讨论是否补后端登录接口 + RBAC，及其与 `X-API-Key` 鉴权的关系
- **产品决策（userId 应由登录派生）**：用户明确 userId 应是「每个登录用户唯一对应的标识，1:1」，由登录自动派生，而非在系统设置手动配置。当前 `Settings` 页 `/identity` 子路由（本地用户身份，配置 userId）应移除，userId 改为从登录态自动注入（`AppConfig.userId` ← 登录返回）
- **待办**：后端补真实登录接口（登录返回 `{user, token, userId}`）→ 前端登录后把 userId 写入 authStore/AppConfig → 移除 `Settings` 的 userId 手动配置项
- **状态**：`[~]`

### 7. 次要问题

- `getAdminStats` 在 `admin.ts` / `dashboard.ts` 重复导出
- 前端 `MemorySearchPayload` 缺 `keyword` 字段（后端 schema 有）
- 前端 `MemoryWritePayload` 多 `agent_id` 字段（后端从 header 取，不读 body）
- **状态**：`[ ]`

### 8. 智能体注册：字段匹配，但 scene_id 手填存在 404 隐患

- **字段核对**：`agent_name` / `scene_id` / `permissions` 三字段前端都传、后端都收，契约匹配（✅）
- **隐患**：前端 `AgentAccess/index.tsx` 的 `scene_id` 是手填 Input；后端 `agent.py:50-54` 注册时硬校验 scene 存在且 `is_active`，否则 404「场景不存在或已停用」。前端无 scene 下拉、无提交前预校验
- **影响**：用户手填 scene_id 拼错 / 场景未建 / 已停用时注册失败
- **状态**：`[ ]`

### 9. 场景管理页面与后端接口完全匹配（已核对）

- 前端 `SceneManagement/index.tsx` 三个操作与后端 `scene.py` 一一对应、契约一致：
  - 创建 `POST /api/v1/scene`（`{scene_name, description}` ↔ `SceneCreateRequest`）✅
  - 列表 `GET /api/v1/scene?page=&page_size=` ↔ `{items,total,page,page_size}` ✅
  - 停用 `DELETE /api/v1/scene/{scene_id}` ✅
- 两处无害小差异：前端 `SceneCreatePayload` 无 `extra_meta`（后端可选）；前端 `SceneInfo` 声明了 `extra_meta?` 但后端未返回（前端未用到）
- **结论**：场景页面可直接用，无前后端不一致
- **状态**：`[x]`（已核实，无问题）

### 10. 智能体记忆权限（read/write）未真正生效

- **现象**：前端注册页勾选「读取/写入记忆」权限，但后端不消费该值
- **证据链**：
  - `agent.py:70` 仅存储 `permissions` 到 `Agent.permissions`（JSON）
  - 唯一检查 `permissions` 的是 `deps.py:218-220` 的 `require_admin`，且只认 `"admin"`
  - `memory/write`、`/search`、`/list`、`/update`、`/delete` 等所有读写端点**均无 read/write 权限校验**
  - 前端权限选项只有 read/write（无 admin），`require_admin` 永不触发
  - 当前 `settings.yaml:105` `auth.enabled=false`，`deps.py:53-67` 开发模式连 API Key / is_active 都不校验
- **结论**：read/write 权限字段纯属摆设；即便 `auth.enabled=true` 也只校验 API Key + is_active，不校验 read/write
- **状态**：`[ ]`

### 11. 接口密钥配置：轮换契约匹配，但存在 rotate-key 越权隐患

- **轮换 API Key**：前端 `rotateAgentKey` → `POST /api/v1/agent/{agent_id}/rotate-key`（无 body）；后端 `agent.py:214` 返回 `{agent_id, api_key, api_key_prefix}`，前端用 `result.api_key` —— 契约匹配 ✅
- **保存凭据**：前端 `CredentialManagement` 仅存浏览器 localStorage，不调后端；后端无「验证 api_key 有效性」接口，手动填错不报错
- **隐患①（越权）**：`agent.py:215-218` 鉴权用 `get_current_agent`（X-API-Key 查出「当前 agent」），但 rotate 目标是 path 里的 `agent_id`，**未校验「path agent_id == 当前 key 所属 agent」**。生产模式下任意持有效 key 的 agent 可轮换他人 key（B 被 A 顶替）
- **隐患②**：手动输入的 apiKey 无后端校验接口（当前 `auth.enabled=false`，后端本就不校验）
- **状态**：`[ ]`

### 12. 「数据校验与标准化」页面无后端对应，应彻底移除（方案 B）

- **现状**：`DataValidation/index.tsx` 是纯前端本地解析+校验+预览，不调任何后端接口；后端无独立「校验/标准化」端点（校验逻辑内嵌在 `/memory/write` 内部，`validation_service.py`）
- **问题**：
  - 校验规则与后端不一致（role 白名单：前端 `user/assistant/system` vs 后端 `user/agent/system/tool/assistant`）
  - 与「记忆数据导入」（Ingestion）功能重叠（Ingestion 已含解析+校验，且能真正写入）
  - 前端内部矛盾：`route-config.tsx:127` 已标 `showInMenu: false`，但 `route-config.tsx:397` 又手动加回菜单
- **决策**：方案 B 彻底移除（仅记录，暂不执行代码）
  - 删 `route-config.tsx:397` 菜单项 + `route-config.tsx:120-128` 路由项 + `LazyRoutePages.tsx` 的 `DataValidation` 引用 + `DataValidation/` 页面文件
- **状态**：`[ ]`

### 13. 「智能体级」记忆层级前后端语义错位

- **后端**：`memory_scope` 四级 `user/session/task/agent`（`models/base.py:185-188`），`/memory/list` 支持 `memory_scope` 过滤
- **前端**：`MemoryScope = 'all' | 'user' | 'session' | 'task'`（`Memory/types.ts`）缺 `agent`；`getMemoryScope`（`Memory/index.tsx:92-97`）对「智能体级」菜单 path 返回 `all`，加载时 `memoryScope: scope === 'all' ? undefined : scope` → 查全部记忆
- **后果**：菜单「智能体级记忆」实为「全部记忆」视图；后端 `memory_scope='agent'`（process/correction 等 agent 私有记忆）前端无入口消费
- **状态**：`[ ]`

### 14. 多层记忆管理：后端仅通用层级接口，无每层独立接口，三个层级视图函数是死代码

- **后端提供的层级接口**（通用，靠参数区分层级）：
  - `POST /memory/list?memory_scope=user/session/task/agent`（`memory.py:750`，走 `memory_store.list_memories` 过滤）
  - `GET /memory/stats`（`level_distribution` 四级统计，`memory.py:863` 走 `count_by_scope`）
- **后端没有**：每层独立的专门接口（无 `/memory/user-level`、`/session-level`、`/task-level`、`/agent-level`）
- **死代码**：`memory_service.py` 的 `get_user_profile`（用户级视图，:291）/ `get_session_context`（会话级视图，:308）/ `get_task_view`（任务级视图，:324）被 import（`memory.py:59-61`）但无任何 `@router` 端点调用；其中 `get_user_profile` 大概率已被 L3 的 `/memory/profile`（`generate_persona`）取代
- **状态**：`[ ]`

### 15. 「记忆生成与去重融合」10 个预设视图：3 个分析型无接口纯假数据，7 个文本型演示数据兜底

- **结构**：`constants/generation.ts` 定义 11 个 view（1 总览 `all` + 10 子预设），实为同一 `GenerationPage` 通过 `?view=xxx` 切换，非 10 个独立页面
- **后端接口**：`generation.py` 有 `generate` / `generate/batch` / `generate/async` / `generate/{id}/status` 四个，仅覆盖「文本生成」链路
- **3 个分析型 view（preference / fact / entity）无接口**：`AnalysisWorkbench` 走 `runAnalysis`（`analysis.ts:156`），用 `seedFromText` / `pickBySeed` 前端哈希随机生成偏好标签/维度/实体；注释明说「当前为前端演示数据兜底；后端分析接口就绪后在此切换真实调用」
- **7 个文本型 view 演示数据兜底**：`demo-results.ts` 注释「后端 DEEPSEEK/SILICONFLOW Key 为空时 `/memory/generate` 无法产出真实结果…构造演示明细…向老师演示」；页面进入即 `buildDemoGenerationResult` 填充假结果，仅提交真实文本 + 后端 Key 已配才返回真实结果
- **结论**：真实可用仅「文本生成」一条链路（依赖 LLM Key）；分析型无后端支撑，纯前端演示
- **状态**：`[ ]`

### 16. 多信号融合检索：接口已协调可用，但存在演示数据兜底 + memory_scope 缺失 + 信号展示与实际不符

- **协调项（✅）**：`searchMemories` 用 `POST /memory/search`，方法一致；参数匹配；响应 `{query,results,total_candidates,elapsed_ms}` 匹配；`buildMemoryTypeFilter`（`utils/memory.ts:8`）已把 `task` → `task_state` 兼容
- **演示数据兜底**：`Retrieval/index.tsx:25-52` `buildMockRetrievalResult`，后端空结果或失败时用假数据填充并标「演示结果」
- **memory_scope 缺失**：前端 `AllDashboard` 显示 `item.memory_scope`（`DifferentiatedResult.tsx:271`），但后端 `memory_search` 的 results 项（`memory.py:430-444`）不返回 `memory_scope` → 真实结果层级列恒为 `—`
- **6 个 mode 信号展示与实际不符**：前端分 semantic/keyword/metadata/fusion/topK 六模式，但后端仅 1 个 `/memory/search`（Qdrant 向量 + 桥接 + 固定权重重排）；keyword 模式暗示 BM25 但后端无关键词信号；fusion 模式权重硬编码（0.4/0.2/0.15/0.25，`DifferentiatedResult.tsx:153-158`）与后端公式（向量0.6 + 时间0.15 + importance0.15 + confidence0.1）不一致
- **状态**：`[ ]`

### 17. 记忆上下文返回：接口真实协调可用，但存在三个小瑕疵

- **协调项（✅）**：`getMemoryContext` 用 `POST /memory/context`，方法一致；参数匹配；响应 `{formatted_text, memory_count, estimated_tokens}` 匹配；无演示数据兜底（纯真实对接）
- **瑕疵① fragments 缺失**：前端 `JsonTree` 用 `result.fragments ?? []`（`DifferentiatedResult.tsx:33`），后端 `memory_context` 只返回三字段、无 `fragments` → json 模式「片段树」恒空
- **瑕疵② group_by_type 不生效**：前端「分组/平铺」开关传 `group_by_type`（`Context/index.tsx:139-141`），后端 `memory.py:633-644` 写死分组（`TYPE_PRIORITY` + `GROUP_TITLES`），不读该参数
- **瑕疵③ include_* 三参数无人消费**：后端 `ContextRequest` 有 `include_preferences/include_facts/include_task_state`，`memory_context` 实现未用；前端类型有但页面未传
- **状态**：`[ ]`

### 18. 接口与监控页面：主表格协调，但 health 缺 app/version、api-logs 缺 trace_id

- **协调项（✅）**：`getAdminApiLogs` 参数（api_path/error_code/hours/page/page_size）与主字段匹配；`getAdminRetrievalLogs` 参数（agent_id/hours/page/page_size）与主字段匹配
- **瑕疵① health 缺 app/version**：前端 `getHealth` 调 `/api/v1/health` 期望 `app/version/status`（`Monitoring/index.tsx:118,124`），但后端 `/api/v1/health`（`main.py:137-139`）只返回 `{status, database}`；`app/version` 在 `/health`（`main.py:132-134`）→ 前端「应用名」「接口版本」恒显示 `-`
- **瑕疵② api-logs 缺 trace_id**：前端 `failureLogColumns` 有「Trace ID」列（`Monitoring/index.tsx:88-91`），后端 `admin_api_logs` items（`admin.py:158-163`）不返回 `trace_id` → 失败调用表格 Trace ID 列恒空
- **状态**：`[ ]`

### 19. 系统设置界面：baseUrl 保留，userId 手动配置应移除（改为登录派生）

- **当前结构**：`Settings/index.tsx` 三个子路由 —— `/connection`（基础连接，baseUrl）、`/identity`（本地用户身份，userId）、默认（baseUrl+userId），均走 `ConfigForm` 本地写入 `AppConfig`
- **baseUrl（基础连接设置）**：✅ 保留 —— 多环境连接切换（本地/远程/测试）所需
- **userId（本地用户身份）**：❌ 应移除 —— userId 是记忆数据隔离标识，应由登录自动派生（每个登录用户 1:1 唯一对应），而非手动配置；当前手动配置与登录体系割裂
- **关联**：问题 #6（登录 mock 无用）的延伸，完整方案见 #6 的「产品决策 + 待办」
- **状态**：`[ ]`

---

### 20. Dashboard `latest_context` 恒为 null —— 三处依赖但源头断裂的死链

- **前端**：`types.ts:172` `AdminDashboardResult.latest_context`、`types.ts:670` `DashboardResult.latest_context`
- **后端死链**：
  - `memory.py:540-683` `/context` handler **全程未设置** `request.state.context_snapshot`
  - `middleware/api_log.py:137-141` 读取该属性拼入 `request_params`
  - `services/dashboard_service.py:689-750` 要求 `request_params.context_snapshot.version == 1` 才返回快照
- **影响**：链条源头从未写入 snapshot → `latest_context` 恒为 null → 前端上下文卡片永远空白，且不报错难察觉
- **状态**：`[ ]`

### 21. 前端未同步「extraction_types 已删除」

- **前端**：`Generation/index.tsx:411` 仍发送 `extraction_types`；`types.ts:317,329` 仍声明该字段
- **后端**：侧支 1 已删除 `extraction_types` 死参数（`schemas/generation.py` 无此字段），Pydantic v2 静默忽略
- **影响**：抽取类型选择器形同虚设；前后端变更不同步，前端待同步删除该字段
- **状态**：`[ ]`

### 22. `memory_profile.py` 是死文件（旧版 /profile 未注册）

- **后端**：`app/api/v1/memory_profile.py` 是旧版 /profile（查 L1 的 preference+fact 生成「结构化画像+文本总结」），但 `router.py` **未 import 它**（未注册）
- **真正生效**：`memory.py:924` 的 L3 新版（消费 L2，返回 `{persona, scene_id, changed_scenes}`）
- **影响**：两个 `/profile` 实现并存易混淆；`memory_profile.py` 应删除（同类：async_write 死代码、extraction_types 死参数）
- **状态**：`[ ]`

### 23. 低优先级代码卫生（10 项，可批量顺手修）

| # | 问题 | 影响 |
|---|------|------|
| 1 | 多个 update/delete 接口类型化为 `string`，后端返回 object | 类型不精确 |
| 2 | 后端已有但前端未集成：`compress`、`context/complete`、`monitor/stats`、`admin/memories` | 前端功能空白 |
| 3 | `dashboard.ts` 死模块（重复实现 getAdminStats） | 维护易改错 |
| 4 | `listAllMemories`/`deleteAllMemories` 未使用导出 | 死代码 |
| 5 | `updateTaskProgress` URL 缺 `encodeURIComponent` | taskId 特殊字符时路径破坏 |
| 6 | `TaskProgressResult` 缺 `progress`/`last_activity` | 进展面板数据不全 |
| 7 | `AdminRetrievalLogItem` 声明后端不返回的字段 | 表格列恒空 |
| 8 | `AdminRecentRetrievalItem.content` 后端从不返回 | 内容恒空白 |
| 9 | `AdminStatsResult` 重复声明 | 维护成本 |
| 10 | search 结果不返回 `memory_scope` | AllDashboard 该列恒「—」 |

- **澄清**：低 2 里的 `proxy` 端点（`/proxy/{space_id}/v1/chat/completions`）前端未集成是**有意的**（给外部 agent 用，非管理面板），不算问题
- **状态**：`[ ]`

---

## 二、前端功能取舍（必须 vs 冗余 vs 待定）

> 2026-08-19 讨论。基于逐页检查结论，梳理前端功能的保留/移除方向，作为后续改动依据。

### 必须保留（有后端接口支撑，业务核心）

| 页面/功能 | 后端接口 | 现状 |
|---|---|---|
| 智能体注册接入（AgentAccess） | `/agent/register` | ✅ 可用，scene_id 手填需改下拉（#8） |
| 场景标识配置（SceneManagement） | `/scene` 全套 | ✅ 完全匹配（#9） |
| 接口密钥配置（CredentialManagement） | `/agent/{id}/rotate-key` | ✅ 轮换可用（#11 越权待修） |
| 记忆数据导入（Ingestion） | `/session` + `/memory/write` | ⚠️ write 响应过时致崩溃（#2） |
| 多层记忆管理（Memory 4 层级） | `/memory/list` + `/stats` | ⚠️ list 405（#1） |
| 会话/任务管理（Session/Task） | `/session` `/task` 全套 | ✅ 匹配 |
| 多信号融合检索（Retrieval） | `/memory/search` | ✅ 协调（#16 演示数据/错位待清理） |
| 记忆上下文返回（Context） | `/memory/context` | ✅ 协调（#17 小瑕疵） |
| 接口与监控（Monitoring） | `/health` + `/admin/api-logs` 等 | ✅ 主表格匹配（#18 字段缺失） |
| 系统总览（Overview/Dashboard） | `/admin/dashboard` | ⚠️ latest_context 死链（#20） |

### 冗余 / 应移除（无后端支撑 或 假数据 或 重复）

| 页面/功能 | 判断依据 |
|---|---|
| 数据校验与标准化（DataValidation） | 纯前端本地校验，无后端接口，与 Ingestion 重复 —— 方案 B 移除（#12） |
| 生成与去重融合 · 分析型 3 view（偏好/事实/实体提取） | `runAnalysis` 纯前端假数据，无后端接口（#15） |
| 忘记密码（ForgotPassword） | 登录本身 mock，忘记密码更无后端支撑 |

### 待定（需补后端 or 产品决策）

| 页面/功能 | 结论（2026-08-19 用户拍板） |
|---|---|
| 登录（Login） | ✅ **保留**，补后端登录接口 + userId 由登录派生（#6） |
| 记忆画像展示页（L3） | ✅ **保留**，补对接 `/memory/profile`，新增画像展示页（#5） |
| 系统设置（Settings） | ✅ baseUrl **保留**，userId **删除**（改登录派生，#19） |
| 生成与去重融合 | ⏳ **进一步讨论**（#15） |
| 检索 6 mode | ⏳ **进一步讨论**（#16） |

### 结论

前端大量「为了演示而做的展示层」（生成 10 预设、检索 6 mode、数据校验页）底层只有少数真实接口，其余是假数据或重复包装。判断标准：① 有真实接口且业务必需 → 保留修 bug；② 纯演示/假数据/重复 → 冗余收敛或移除；③ 产品要求有但无接口 → 补后端（登录/画像/分析型提取）。

---

### 指导者检查（前端功能取舍）

> 标记为「指导者」外部检查。分类框架（保留/冗余/待定）是对的，判断标准清晰，但有一个归类错误需修正。

**① 修正：画像页（Profile）应「待定补对接」，不是「冗余移除」**：

画像（L3）是四层记忆的**核心产出**（L0→L1→L2→L3 最顶层），后端 `/memory/profile` **有真实接口**（L3 新版，消费 L2，返回 persona 自由文本），只是前端没接。所以画像是"核心功能还没接上"，应**补前端对接**，而非移除——与"分析型 3 view"（后端根本没接口）是两回事。

**② 更根本的判断视角：回到「系统给谁用、核心价值是什么」**：

| 核心价值 | 谁用 | 前端对应功能 |
|------|------|-------------|
| 记忆存储与检索（L0-L3） | 外部 agent（走 Proxy） | 记忆查看、检索、画像 |
| 接入管理（agent/scene 配置） | 管理员/运营 | 注册 agent、建 scene、管密钥 |
| 治理（鉴权 + 隔离） | 管理员 | 密钥管理、场景隔离配置 |
| 可观测（健康、日志） | 运维 | 监控页 |

前端（管理面板）的必要功能 = 上述四类；不必要 = "为了演示而做的展示层"（假数据、重复包装、无后端）。

**③ 明确的「必要 vs 不必要」清单**：

- **必要（有真实接口 + 业务核心）**：agent 注册 / scene 管理 / 密钥管理、记忆导入 / 多层记忆管理、记忆检索 / 上下文、**画像页（补对接，勿移除）**、会话/任务管理、监控 / 系统总览。
- **不必要 / 移除**：数据校验页（与导入重复）、生成·分析型 3 view（无后端假数据）、忘记密码（登录 mock 无后端）。
- **待定**：登录（补后端登录 + userId 派生）、生成·文本型 7 view（收敛 or 去演示）、检索 6 mode（收敛 or 补后端）、系统设置（userId 改登录派生）。

**核心判断标准**：对应系统核心价值（记忆/接入/治理/可观测）→ 必要（修 bug 保留）；演示/假数据/重复 → 移除；产品要求但无接口 → 补后端。

---

## 三、讨论记录

### 2026-08-19 · 生成与去重融合 + /memory/generate 定位

**① 5 种记忆类型确认（抽取 → 落库链路）**：

| 阶段 | 文件 | 结论 |
|---|---|---|
| 抽取 | `memory_extractor.py` | 一次抽 5 类：key_fact / task_state / preference / process / feedback |
| 生成 | `memory_generator.py` | `VALID_MEMORY_TYPES = {fact, preference, task_state, process, correction}`，LLM 映射成 5 个 memory_type |
| 落库 | `memory_service.py` → `t_memory.memory_type` | 每条记忆携带 memory_type |

对应关系：`key_fact→fact`、`preference→preference`、`task_state→task_state`、`process→process`、`feedback→correction`。**记忆确实按 5 类抽取、落库带类型。**

**② 产品方向（用户拍板）**：
- 「生成与去重融合」预设 view **不对外提供**（前端 10 预设收敛）。
- 「5 种类型」由**检索承载**：对外提供按 `memory_types` 检索（后端 `/memory/search` 已支持）。

**③ /memory/generate 接口定位**：
- 同步完整流水线（extract→generate→dedup→store），输入文本直接返回记忆明细。
- vs `/memory/write`：write 落 L0 + 异步抽取（**主链路**）；generate 跳过 L0 同步抽（**旁路**）。
- 问题：generate 跳过 L0，`source_record_ids` 通常为空，**溯源弱**；批量导入应走 write（保留 L0），而非 generate。
- 建议：`/memory/generate` 对外接口**移除**；`memory_pipeline.run` 核心函数**保留**（write 的 L1 worker 依赖）。

**④ 待确认**：generate 是「前端收敛（后端接口保留）」还是「前后端都删 generate 接口」（用户尚未明确）。

### 2026-08-19 · 检索模块：mem0 三路检索机制 vs 当前系统现状

**① mem0 的三路检索（读源码 `MEM0架构解析.md` 确认，`search()` 分 9 步）**：
- 语义检索：embed query → 向量相似度检索（over-fetch 扩候选池）
- 关键词检索：`keyword_search`（BM25，Qdrant 用稀疏向量实现）
- 实体 boost：query 抽实体 → 查独立 entity store → 对关联记忆加权

**② 实体 store 机制**：
- 独立于主 collection 的第二个 collection
- 写入时（add Phase 7）：抽实体 → 精确/语义匹配（score≥0.95）→ 建「实体→linked_memory_ids」关联
- 检索时（Step 6）：query 抽实体 → 查实体 store → boost 关联记忆
- 实体记录：`{data:"实体名", entity_type:"person/...", linked_memory_ids:[...]}`

**③ 关键发现：当前系统基本没用 mem0（grep 确认）**：
- `mem0_client.add` **无任何业务代码调用**（写入是自研 write→L0→memory_pipeline）
- `mem0_client.search` 只在 `retrieval_service.py:117` 调用，但 `retrieval_service` **无路由 import（死代码）**
- 主链路 `/memory/search` 用自研 Qdrant 直查（`_qd.search_similar`），**未用 mem0**

**④ 结论**：用户判断「实体检索空白」正确，且更准确是——**BM25 关键词检索也空白，主链路只有语义向量一路**。根因：mem0 框架没被主链路用起来（写入自研、检索自研、mem0_client 死代码），而 mem0 的三路检索依赖其 add 写入链路建实体 store，自研写入链路没建。

**⑤ 三条路线（待用户定）**：
- A：真正用起 mem0（写入/检索切回 mem0，放弃自研 pipeline）
- B：自研补关键词（BM25/PG 全文）+ 实体（自建实体表/向量）
- C：混合（自研 pipeline 保留，实体复用 mem0 `entity_extraction`，关键词用 Qdrant 稀疏向量）

### 2026-08-19 · 三路检索方案（用户拍板：必须至少三路）

> 用户明确：**必须至少包含三路检索**（语义 + 关键词 + 实体 boost），不是可选优化。路线选 **C（混合）**。

**① 路线选择**：C（混合）——A 会放弃自研 L0-L3 蒸馏链（核心价值）+ 大量返工，排除；B/C 的区别是实体抽取自己写还是复用 mem0，倾向 C（但实体抽取建议自研而非复用 mem0，见下）。

**② 三路的具体实现**：

| 路 | 实现 | 状态 |
|----|------|------|
| 语义检索 | 自研 Qdrant 语义向量（`search_similar`） | ✅ 已有，保留 |
| 关键词检索 | **Qdrant sparse vector**（jieba 分词 + BM25 权重 → 稀疏向量） | ❌ 补，成本低收益大 |
| 实体 boost | **自研实体抽取**（L1 抽取时顺带抽实体）+ 独立实体表（entity → linked_memory_ids）+ 检索加权 | ❌ 补，成本高收益中 |

**关于实体抽取自己写 vs 复用 mem0**：mem0 的 `entity_extraction` 在死代码里，复用需理解并摘出、且依赖 mem0 的 LLM 封装，**不如自研干净**。自研实体抽取：在 L1 抽取时顺带抽实体（L1 结构化输出加 `entities` 字段），或单独 LLM 抽实体。

**③ 实施顺序（分两步）**：

| 步骤 | 内容 | 成本 | 收益 |
|------|------|------|------|
| 第一步 | 关键词检索（Qdrant sparse vector + jieba） | 低 | 大（补"字面匹配"缺口，解决大部分漏召回） |
| 第二步 | 实体 boost（NER + 实体表 + boost） | 高 | 中（提升实体相关召回） |

**④ 三路融合方式**：

- 语义 + 关键词：**RRF（Reciprocal Rank Fusion）** 融合（rank 融合，避免分数量纲不一致，参考 TencentDB 做法）。
- 实体 boost：在融合后的结果上**加权**（实体命中的记忆提升排名）。

**⑤ 一个必须澄清的前提**：三路结果要**融合**（合并排序，非各自返回），融合方式要设计（对应 #16 "fusion 权重硬编码与后端公式不一致" 的历史问题）。

### 2026-08-19 · 为什么实体自研而非复用 mem0

> 之前"死代码、复用不干净"的说法太表面。真正核心原因：**mem0 的 entity_extraction 不是"独立可插拔组件"，而是 mem0「写入链路（add）的耦合零件」**。

**三个维度**：

1. **耦合性**：mem0 实体抽取是 `add` 写入链路的 Phase 7（共 9 Phase），依赖 Phase 1-6 的产物（记忆 id/embedding/分块）。复用 Phase 7 要理解整个 add 链路 + 摘出 + 补全前置依赖——"不是摘一个函数，是拆一段流水线"。

2. **依赖**：mem0 的 entity_extraction 依赖 mem0 的 LLM 封装、配置体系、向量存储封装。当前是自研架构（memory_pipeline + Qdrant 直查），复用 mem0 等于为"抽实体"一个功能引入 mem0 半套依赖（而 mem0 在代码里是死代码）。

3. **自研成本远低**：实体抽取本质是"从文本抽实体（人名/地名/产品名/订单号）"，自研只需"一个 LLM prompt + 一张实体表（entity→linked_memory_ids）+ 一个关联逻辑"，几十行代码；复用 mem0 的成本（理解+摘出+适配+引入依赖）远高于此。

**类比**：mem0 的 entity_extraction 是"发动机里的活塞"（只在 add 链路里运转），不是"货架上单买的螺丝"。自造一个简单螺丝（一个 prompt + 一张表），比拆发动机取活塞便宜得多。

**自研实体抽取的实际方案（成本很低）**：

1. 抽取：L1 抽取时顺带抽实体（L1 结构化输出加 `entities: [{name, type}]` 字段），或单独 LLM 调用抽实体。
2. 存储：独立实体表 `entity → linked_memory_ids`（或 Qdrant 第二个 collection）。
3. 检索：query 抽实体 → 查实体表 → boost 关联记忆。

**额外收益**：自研可**定制业务实体类型**（订单号/产品名/项目名），mem0 的通用 person/entity 反而不一定能抽出这些。

### 2026-08-19 · 修正：不用自研三路，改「自研 L0-L3 + mem0 三路检索」并存

> **修正声明**：上文「三路检索方案（自研关键词+实体）」和「为什么实体自研而非 mem0」的结论**作废**。用户质疑到位——mem0 三路检索开箱即用、调优过，自研效率大概率更低。

**修正后的架构**（自研和 mem0 并存，非二选一）：

```
写入：write → 落 L0 → L1 worker 抽取（自研 L0-L3 蒸馏链保留）
            → 【新增】L1 记忆 add 到 mem0（建语义 + 关键词 + 实体 store 索引）
            → L2/L3（自研不变）

检索：search → mem0 search（三路：语义 + 关键词 + 实体 boost，开箱即用）
```

| 层 | 谁来做 |
|----|--------|
| 记忆组织（L0-L3 蒸馏） | 自研（保留，核心价值） |
| 记忆存储 + 三路检索 | **mem0（开箱即用，效率高）** |

**三个澄清**：

1. **Qdrant 不是仅向量检索**：支持稀疏向量（sparse vector，1.7+）做关键词检索（BM25 类）+ 混合检索（hybrid）。且 mem0 的关键词/实体检索底层很可能就是用 Qdrant 实现（sparse vector + 第二 collection）。

2. **实体检索（图检索）自研效率大概率低于 mem0**：mem0 的实体抽取 prompt、实体匹配（score≥0.95）、boost 权重都是调优过的，自研要从零实现+调优，召回质量大概率不如 mem0。

3. **集成点只有一个**：L1 worker 抽出 L1 记忆后 add 到 mem0 建索引；检索用 mem0 search 三路。

**需确认的前提**：mem0 的 `add` 是否支持「外部传入已抽取的记忆」（而非"传原始消息让 mem0 自己抽"）。若支持（如 `add(memory=...)`），自研 L1 记忆直接喂 mem0 建索引；若只支持"传消息自己抽"，需把 L1 记忆"当消息传"（绕过 mem0 抽取）。

### 2026-08-19 · 三路检索实施计划（分两步）

**技术可行性确认（Qdrant 能力）**：
- Qdrant **不是只支持向量检索**，同时支持 dense（语义）+ sparse（BM25 关键词）两种向量。
- mem0 的 `qdrant.py` 已实现范例：`fastembed` 的 `SparseTextEmbedding(model_name="Qdrant/bm25")` 生成 BM25 稀疏向量，存进 collection 的 `bm25` sparse vector slot。
- **中文注意**：`Qdrant/bm25` 是英文分词，中文需 **jieba 分词 + 自算 BM25 稀疏向量**（词→词频权重→稀疏向量）。

**第一步：关键词检索（低成本高收益，先做）**

1. 引入 `jieba` 依赖
2. Qdrant collection 增加 **sparse vector slot**（BM25）
3. 写入侧：记忆内容 → jieba 分词 → BM25 权重 → 稀疏向量，与 dense 一起写入
4. 检索侧：query → jieba 分词 → 稀疏向量 → Qdrant sparse search
5. 融合：语义（dense）+ 关键词（sparse）用 **RRF** 融合

**第二步：实体检索（entity boost，中成本）**

1. 实体抽取：L1 抽取时顺带抽实体（`Memory.entities` 字段**已有基础**，generator LLM 已输出 entities）
2. 建实体索引：独立实体表 `entity → linked_memory_ids`（从已有 `Memory.entities` 反查建表）
3. 检索侧：query 抽实体 → 查实体表 → 命中关联记忆 **boost**
4. 融合：在「语义+关键词」RRF 结果上，实体命中记忆加权

**融合总览**：

```
query
 ├─ 语义检索（dense）──────┐
 ├─ 关键词检索（sparse）────┼─ RRF 融合 → 基础排序 → 实体 boost → Top-K
 └─ 实体抽取 → 实体表查询 ──┘
```

**好消息**：`Memory.entities` 字段已存在（generator LLM 已输出），实体检索成本比预估低——只需建索引表 + boost 逻辑，不用从零抽实体。

### 2026-08-19 · 检索最终决策：放弃 mem0，自研三路，未来可换向量库

**决策（用户拍板）**：**不使用 mem0，使用自研三路检索**。

**放弃 mem0 的理由（转用 mem0 检索的完整代价）**：

> 核心：mem0 的检索和写入是绑定的（三路索引都是 add 时建的），转检索就得连带转写入。没有「只转检索、不碰写入」的选项。

1. **代价 1（最大）：要么放弃自研 L0-L3，要么双写**
   - 完全切 mem0 = 放弃 L0-L3 分层蒸馏、自研去重融合（keep_new/merge/discard + 版本历史 + 审计）、memory_scope 四级隔离、记忆溯源 —— 这是「多智能体改造方案」的核心价值
   - 双写 = 存储翻倍 + 一致性维护

2. **代价 2：LLM 成本翻倍（双写时）**
   - 建实体 store 必须 `infer=True`，而 `infer=True` 会用 mem0 的 `ADDITIVE_EXTRACTION_PROMPT` **重新抽取一遍**
   - 结果：自研 L1 抽一遍 + mem0 再抽一遍，LLM 成本 ×2，且两套抽取结果不一致

3. **代价 3：实体检索仍走不通（最讽刺）**
   - `infer=False` 喂自研 L1 → **不建实体 store**，实体检索仍空白
   - `infer=True` → 建实体 store，但重复抽取
   - **没有「既不复抽、又有实体」的路径**

4. **代价 4：失去定制能力**
   - mem0 实体抽取是通用 person/entity，**抽不出业务实体**（订单号/产品名/项目名）
   - 存储结构、索引、去重（MD5 hash）、抽取 prompt 都是 mem0 自己的，自研定制无法套用

**判断**：三路里语义已有（自研）、关键词补 sparse 是低成本（几十行）、实体自研更可控 —— **没必要为「复用 mem0 开箱即用」付出「放弃核心价值或双写+翻倍 LLM」的代价**。

### 2026-08-19 · 存储层抽象决策 + Oracle 23ai 选型参考

**最终决策（用户拍板）**：
- **做存储层抽象**（单业务层 + 可切换存储层）
- **不维护 mem0 双轨**（放弃「两个写入接口」的想法）

**理由**：
- 未来换向量库是「存储层切换」，用存储抽象层解决，不需要 mem0 作为业务层备选
- 「两个写入接口」= 维护两套业务层（两套抽取/去重/数据），复杂度高且一致性难维护
- mem0 是「业务层 + 存储层」的完整框架，和自研 memory_pipeline 是同一层的竞争对手，不是「存储层备选」

**Oracle 23ai 选型参考（有根据，作为未来候选）**：
- 版本澄清：最新是 **Oracle 23ai**（无 26ai）
- 满足「三合一」：语义（`VECTOR` + HNSW/IVF）+ 关键词（sparse vector 23.8 + Oracle Text）+ 实体/图（SQL Property Graph + `GRAPH_TABLE` + GraphRAG），converged database 一个库全包
- 适配自研：✅ 且能简化（向量+关系同表 → 删 T_MEMORY_VECTOR 桥接表；检索一条 SQL `ORDER BY VECTOR_DISTANCE + WHERE`）
- 代价：商业 license + 部署重 + 迁移成本（数据迁移 + SQL 改写）

**这印证了「存储层抽象」的正确性**：未来从「PG+Qdrant」切到「Oracle 23ai」时，只需改存储层实现，业务层（L0-L3 蒸馏）完全不动。

### 2026-08-19 · Oracle 23ai 选型详情（未来向量库候选）

**版本**：Oracle 23ai（2024年5月 GA，23.8 更新 2025年4月）。无「26ai」版本（用户笔误）。

**能力明细（三合一 + 混合检索，均为原生 converged）**：

| 能力 | Oracle 23ai 实现 |
|---|---|
| 语义检索 | `VECTOR` 数据类型 + HNSW（内存邻居图）/ IVF 索引 + `VECTOR_DISTANCE()`（cosine/dot/euclidean），支持目标精度设定 |
| 关键词检索 | ① **Sparse vector**（23.8 新增原生 PL/SQL sparse 支持 + MLE 自定义距离函数）② Oracle Text `CONTAINS()` 全文索引 |
| 实体/图检索 | **SQL Property Graph** + `GRAPH_TABLE()`（ISO SQL/PGQ 标准）+ 原生 GraphRAG |
| 混合检索 | `DBMS_HYBRID_VECTOR.SEARCH`（+ `SEARCHPIPELINE`/`GET_SQL`/`FILTER_BY`），向量 + `WHERE` 关系过滤在一个执行计划（CBO predicate pushdown，避免后过滤近似） |

**适配自研结构的映射**：

| 自研结构（当前） | Oracle 23ai 对应 | 变化 |
|---|---|---|
| T_MEMORY 关系表 | Oracle 表（同构） | ✅ 兼容 |
| T_MEMORY_VECTOR 桥接表 | **删掉**（embedding 作为 T_MEMORY 的 `VECTOR` 列，向量+关系同表） | ✅ 简化 |
| Qdrant payload（user/scene/task/session） | 本就是 T_MEMORY 的列 | ✅ 简化 |
| 检索（Qdrant 直查 → 桥接 → 取数据） | 一条 SQL：`WHERE memory_type='fact' ORDER BY VECTOR_DISTANCE(embedding, :q)` | ✅ 大幅简化 |

**代价（选型需权衡）**：
1. 商业 license（非开源，vs 当前 PG + Qdrant 免费）
2. 部署复杂度高（vs `docker-compose up -d` 的 PG+Qdrant）
3. 迁移成本：数据迁移 + SQL 改写（自研 `qdrant_client` 直查逻辑换成 Oracle SQL/API）
4. 待验证：23.8 的 sparse vector 稳定性 + Oracle Text 中文分词支持

**结论**：Oracle 23ai 是「三合一」理想候选，能力完全覆盖（语义+关键词+实体/图），且 converged 架构能简化自研（删桥接表、检索一条 SQL）。主要代价是商业 license + 部署复杂度。作为未来选型候选记录在案，当前仍用「PG+Qdrant + 自研三路」。

**来源**：
- [AI Vector Search | Oracle 中国](https://www.oracle.com/cn/database/ai-vector-search/)
- [Oracle AI Vector Search User's Guide - 23.8 Release Update](https://docs.oracle.com/en/database/oracle/oracle-database/23/vecse/april-2025-release-update-23.8.html)
- [Oracle 23ai vs PostgreSQL pgvector for AI | ROSTAN](https://rostantechnologies.com/blog/oracle-database/oracle-database-23ai-vs-postgresql-pgvector-enterprise-ai-comparison)

**当前自研三路实现**：
- 语义检索：✅ 已有（自研 Qdrant dense 向量）
- 关键词检索：补 Qdrant sparse vector + jieba + BM25
- 实体检索：补自研实体表（`Memory.entities` 已有基础）+ boost

**未来选型方向（标注，暂不实施）**：
- 可能换「三合一」向量数据库：同时支持**语义相似度检索 + 关键词检索 + 简单实体检索（即图检索）**
- **工程要求**：检索的存储层做**抽象**，当前 Qdrant 直查逻辑（`qdrant_client.search_similar` + `T_MEMORY_VECTOR` 桥接）要解耦，便于未来切换向量库（避免换库时改业务代码）

### 2026-08-19 · 指导者检查（检索最终决策）

> 标记为「指导者」外部检查。检索决策经过了完整"质疑 → 修正 → 调研 → 定案"闭环，最终回到自研三路，但理由是深入调研后的——尤其"代价 3"是决定性的。

### 做得好的

1. **"放弃 mem0"的 4 个代价分析充分，尤其代价 3 是决定性的**：用户调研发现"infer=False 喂自研 L1 → 不建实体 store；infer=True → 重复抽取（LLM ×2）；没有既不复抽又有实体的路径"——这直接推翻"自研 L0-L3 + mem0 并存"方案里最想复用的"实体检索"这一路，比指导者之前的判断更深入。

2. **"存储层抽象"决策正确**：把"向量库选择"从"业务层问题"降级为"存储层实现细节"，未来换库只改存储层。

3. **Oracle 23ai 选型参考有价值**：能力三合一、适配自研能简化、代价明确（商业 license + 部署重 + 待验证中文分词），且定位准确（未来候选，当前仍 PG+Qdrant）。

### 一个需要明确补充的点

**自研三路的"效率 vs 可控"权衡，要明确接受"效率可能低一点"**：

| 维度 | mem0 | 自研三路 |
|------|------|---------|
| 实体检索效率（召回质量） | 高（调优过） | 可能低（要自己调优） |
| 可控性 / 定制 | 低（通用 person/entity） | 高（可抽业务实体） |
| 不重复抽取 | ❌（infer=True 复抽） | ✅ |
| 不放弃 L0-L3 | ❌（要么放弃要么双写） | ✅ |

最终选自研，本质是"接受效率可能低一点，换取可控 + 可定制 + 不重复抽取 + 不放弃核心价值"。

**一个缓解**：`Memory.entities` 字段已存在（generator LLM 已输出实体），缓解了"实体抽取"成本（只需建索引表 + boost 逻辑）。但"实体匹配 + boost 权重"仍需自研调优，这部分效率可能低于 mem0。

**建议**：自研实体的"匹配阈值 + boost 权重"要**数据驱动调优**（类似检索阈值 0.5 的调法），用真实数据的召回率/精确率校准，不拍脑袋。

### 总结

"放弃 mem0，自研三路"的最终决策合理且深入——4 代价分析充分，尤其"代价 3（实体检索走不通）"决定性，推翻了"并存"方案里最想复用的实体检索一路。"存储层抽象"正确（未来换库只改存储层）。需明确：自研三路的"效率 vs 可控"权衡（接受效率可能低一点，换可控+定制+不复抽+不放弃核心价值），且实体匹配/boost 权重要数据驱动调优。

---

## 四、问题处理方案（按组解决思路）

> 2026-08-19 整理。23 个前后端问题按「相关性」分组，每组给出解决思路，作为实施顺序的依据。

### 第一组：方法/契约错误（硬伤，改前端）

| # | 问题 | 解决思路 |
|---|---|---|
| 1 | list GET vs POST | 前端 `memory.ts` 改 POST（后端/文档/测试均 POST，前端 GET 是 bug） |
| 2 | write 响应过时 | 前端 `types.ts` 的 `MemoryWriteResult` 改 `{accepted, session_id, l0_count, record_ids}`；`Ingestion/index.tsx` 去掉 `result.results.filter` 崩溃逻辑，改显示「已提交 N 条」 |
| 3 | write 超时/注释 | 前端 `memory.ts` 注释改「毫秒级」，`timeout: 60000` 改回默认 |
| 4 | delete 500 workaround | 先确认后端 `/memory/delete` 是否真 500，若已修复则删前端 workaround |

### 第二组：死代码/冗余清理（直接删）

| # | 问题 | 解决思路 |
|---|---|---|
| 12 | 数据校验页 | 删菜单项 + 路由 + 页面（方案 B 已定） |
| 14 | 层级视图死代码 | 删 `memory_service.py` 的 `get_user_profile`/`get_session_context`/`get_task_view` + import |
| 21 | extraction_types | 前端 `Generation/index.tsx` + `types.ts` 删 `extraction_types` |
| 22 | memory_profile.py | 删整个文件 |
| 7 | 次要（getAdminStats 重复等） | 删 `dashboard.ts` 重复导出、补 `keyword` 字段、删多余 `agent_id` |

### 第三组：后端补字段/校验（小改后端）

| # | 问题 | 解决思路 |
|---|---|---|
| 17 | 上下文三瑕疵 | 后端 `memory_context` 补 `fragments` 返回 + 让 `group_by_type` 生效（去掉写死分组） |
| 18 | 监控两字段 | 后端 `/api/v1/health` 补 `app`/`version`；`admin_api_logs` 补 `trace_id` |
| 20 | latest_context 死链 | 后端 `/context` 补 `request.state.context_snapshot`（或前端去掉该卡片） |
| 11 | rotate-key 越权 | 后端 `agent_rotate_key` 补「path agent_id == 当前 key 所属 agent」校验 |

### 第四组：前端体验/语义（改前端）

| # | 问题 | 解决思路 |
|---|---|---|
| 8 | scene_id 手填 | 前端 `AgentAccess` 改成下拉（数据源 `listScenes`） |
| 13 | 智能体级 scope 错位 | 二选一：前端 `MemoryScope` 补 `agent` 级 / 或去掉「智能体级」菜单改语义 |
| 19 | 系统设置 userId | 前端 `Settings` 删 `/identity` 子路由 + userId 字段（已定） |

### 第五组：需补后端（较大，涉及新接口）

| # | 问题 | 解决思路 |
|---|---|---|
| 5 | 画像展示页 | 前端新增「记忆画像」页，调 `/memory/profile` 展示 persona（已定补对接） |
| 6 | 登录 | 后端补 `/auth/login`（返回 user+token+userId），前端登录后注入 userId（已定） |
| 10 | read/write 权限 | **待用户拍板**：后端是否真正实现 read/write 校验（否则前端权限选项是摆设） |

### 单独列出（复杂，需专项处理）

**⭐ 生成与去重融合收敛（#15）**：
- 已定：10 个预设 view 收敛成 1 个「生成」页（去掉 3 分析型假数据 + 7 文本型演示兜底），「5 种类型」由检索承载
- 遗留未定项：`/memory/generate` 接口去留（前端收敛 vs 前后端都删）

**⭐ 检索三路（#16）**：
- 已定：自研三路（语义已有 + 关键词补 Qdrant sparse + 实体补实体表）+ 存储层抽象（未来可换 Oracle 23ai 等三合一库）
- 实施分两步：先关键词、后实体

### 建议执行顺序

| 组 | 问题数 | 难度 | 顺序 |
|---|---|---|---|
| 第一组 契约错误 | 4 | 简单（改前端） | ① 最先 |
| 第二组 死代码 | 5 | 简单（删） | ② |
| 第三组 后端小改 | 4 | 中 | ③ |
| 第四组 前端体验 | 3 | 简单-中 | ④ |
| 第五组 补后端 | 3 | 大 | ⑤ 最后 |
| 生成收敛 #15 | 1 | 复杂 | 单独 |
| 检索三路 #16 | 1 | 复杂 | 单独 |

### 指导者检查（问题处理方案）

> 标记为「指导者」外部检查。分组和执行顺序合理（先易后难、先修 bug 后加功能）。补充三个"二选一/待拍板"项的决策建议。

**做得好的**：

1. 分组合理（按相关性 5 组），执行顺序合理（先易后难、先修 bug 后加功能）。
2. 解决思路具体（落到改哪个文件、删什么、补什么）。
3. 诚实标注待拍板项（问题 10、15）。

**三个决策建议**：

**① 问题 20（latest_context 死链）—— 倾向"补后端"，不要"前端去掉卡片"**：latest_context 有可观测价值（管理员能看到"最近检索了什么上下文"），修复成本极低（/context 补一行 snapshot）。前端去掉卡片是"阉割功能"，不如补后端。

**② 问题 13（智能体级 scope 错位）—— 倾向"前端补 agent 级"，不要"去掉菜单"**：`memory_scope='agent'`（process/correction 等 agent 私有记忆）是治理层 Q13 的核心，前端应有入口让用户看到 agent 私有记忆，去掉菜单等于"隐藏了 agent 私有记忆的入口"。

**③ 问题 10（read/write 权限）—— 建议归入"方案 B 下一迭代"，当前不实现**：治理层已定"方案 A（层级决定可见性，read/write 是方案 B 下一迭代）"，所以 read/write 权限当前不应实现后端校验。建议前端权限选项暂时隐藏或标注"未生效"，等方案 B 迭代再实现（而非提前做方案 B）。

**一个提醒**：问题 4（delete 500 workaround）的"确认"要**实际调接口测试**（POST /memory/delete 看是否 500），而非只看后端代码逻辑——"是否有 500 bug"是运行时行为，代码看着对也可能 500。

---

## 五、检索架构再讨论：mem0 源码确认（[~] 讨论中）

> 2026-08-19。读 mem0 源码后，重新评估"自研 vs mem0"。**结论有重大修正**，标记讨论中。

### 1. mem0 的 add 支持"外部传已抽取的记忆"（infer=False）

`add` 方法有 `infer` 参数：`infer=False` 时不调 LLM，直接存储 messages 的 content。所以自研抽取的记忆可喂 `add(infer=False)`，**不重复抽取**（解除"代价 2"）。

### 2. 重要修正：infer=False 也建"关键词索引"，之前"关键词空白"判断错了

`_create_memory` 里统一建了 `text_lemmatized = lemmatize_for_bm25(data)`（BM25 关键词索引）。所以 infer=False 建了"语义 + 关键词"两路索引，**只缺"实体"一路**（不建实体 store）+ **不做去重**。

| 索引 | infer=False | infer=True |
|------|------------|------------|
| 语义向量 | ✅ | ✅ |
| 关键词 BM25 | ✅ text_lemmatized | ✅ |
| 实体 store | ❌ 不建 | ✅ |
| 去重 | ❌ 不做 | ✅ |

### 3. mem0 不支持"按记忆类型抽取"

三个证据：① `memory_type` 参数只认 procedural_memory（其他报错）；② ADDITIVE_EXTRACTION_PROMPT 输出扁平事实（无 type 字段）；③ `_create_memory` 存储无类型维度。**"按 5 类抽取"是自研 memory_extractor 的核心价值，mem0 替代不了**（且类型是治理层的基础）。

### 4. 修正后的最终架构（讨论中，倾向混合方案）

```
mem0 做：存储 + 语义检索 + 关键词检索（infer=False 喂自研记忆，不重复抽取）
自研做：抽取 5 类（memory_extractor，已有）+ 去重（memory_dedup，已做）
       + 实体（Memory.entities 已有，只补索引表+boost）+ L0-L3/隔离/治理（已有）
```

**要新做的只有两小块**：① 实体索引表（entity→linked_memory_ids）+ boost；② mem0 接入（add infer=False + search）。其余全是已有的。

### 5. 待拍板项

- 是否采用"混合方案"（mem0 存储+语义/关键词检索，自研抽取/去重/实体/L0-L3）？
- 实体检索是否当前做（还是先语义+关键词两路，实体后补）？
- 之前"放弃 mem0 自研三路"的结论是否推翻？

### 状态：`[~]`（讨论中，待用户拍板）

---

### 6. 实体检索完整方案（思路理清，[~] 讨论中）

> 经过多轮讨论，实体检索的思路理清如下。**修正上文的"实体索引表（entity→linked_memory_ids）"——改为"实体存 t_memory.entities（JSONB）+ GIN 索引"，不建独立实体表。**

**实体生命周期：抽取 → 存储 → 检索 → 融合**

**① 抽取（两个时机，都用 LLM）**：

| 时机 | 抽什么 | 频率 | 成本 |
|------|--------|------|------|
| 离线（L1 抽取记忆时） | 记忆的实体 | 每条记忆一次 | 已做（Memory.entities 字段） |
| 在线（检索时） | query 的实体 | 每次检索一次 | 轻量（query 短，复用同一 LLM 能力，可缓存） |

**关键澄清（回应用户两个批评）**：① 实体抽取**用 LLM 不用规则/正则**（场景不确定，实体格式不固定）；② 实体抽取的**大头在 L1 离线抽（记忆实体），在线只抽 query 短实体**，不是检索时才想起抽。

**② 存储：t_memory.entities（JSONB）+ GIN 索引，不建独立表**：

```sql
ALTER TABLE t_memory ADD COLUMN entities JSONB DEFAULT '[]';
CREATE INDEX idx_memory_entities ON t_memory USING GIN (entities);
```

**为什么存 t_memory 不建独立表**：实体是"记忆的属性"，和记忆一起存，天然关联、天然一致、少一层查询。

**③ 检索三步**：

```
query = "帮我查 DH001 订单的退货情况"
Step 1 抽实体：LLM(query) → ["DH001"]
Step 2 查记忆：SELECT memory_id FROM t_memory WHERE entities @> '["DH001"]'（GIN 索引加速）
Step 3 boost：命中的 memory_id，score += BOOST_WEIGHT
```

**④ 融合：语义 + 关键词 RRF，实体 boost 加权**：

```
query
 ├─ 语义检索（dense）──────┐
 ├─ 关键词检索（sparse）────┼─ RRF 融合 → 基础排序 → 实体 boost → Top-K
 └─ 实体（query 抽 → @> 匹配）┘                ↑ 命中实体的记忆加权
```

**完整流程图**：

```
【离线】L1 抽取记忆
  消息 → LLM → 记忆 content + 实体 entities → 存 t_memory（entities JSONB + GIN 索引）

【在线】检索
  query → LLM 抽实体 → ["DH001"]
        ├─ 语义检索（dense）──────┐
        ├─ 关键词检索（sparse）────┼─ RRF 融合 → 基础排序
        └─ 实体 @> 匹配（GIN）────┘              ↓ 实体命中加权 → Top-K
```

### 状态：`[~]`（讨论中）
