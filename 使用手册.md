# 智能体记忆系统 — 使用手册

> 面向新成员的启动与使用指南。读完这份文档，你就能从零把前后端跑起来，并完成「接入智能体 → 写入记忆 → 检索 → 画像」的完整链路。

---

## 一、项目简介

这是一个**面向大模型智能体的记忆系统**，让 AI 智能体在多轮、跨会话、多场景的交互中「记住」用户，实现连续、个性化的对话体验。

核心能力：**接入写入 → 建模分层 → 生成去重 → 多路检索 → 上下文返回**。

- 记忆分四层：`L0 原始对话 → L1 原子记忆 → L2 场景块 → L3 用户画像`
- 记忆分 5 类：`fact（事实）/ preference（偏好）/ task_state（任务状态）/ process（流程）/ correction（纠正）`
- 检索：语义（dense）+ 关键词（sparse）双路 hybrid 融合

---

## 二、技术栈

| 层 | 技术 |
|----|------|
| 后端 | FastAPI + SQLAlchemy(async) + PostgreSQL(pgvector) + Qdrant + Kafka |
| 前端 | React 19 + TypeScript + Vite + Ant Design |
| LLM | DeepSeek（deepseek-chat） |
| Embedding | SiliconFlow（BGE-M3，1024 维） |
| 部署 | Docker Compose |

---

## 三、目录结构

```
claudecode/
├── memProject/              # 后端（FastAPI）
│   ├── app/                 # 应用代码
│   ├── alembic/             # 数据库迁移
│   ├── docker-compose.yml   # PG + Qdrant + Kafka
│   ├── .env                 # 环境变量（密钥等）
│   ├── config/settings.yaml # 全局配置
│   └── 开发日志.md          # 开发记录
└── agent-memory-frontend/   # 前端（React + Vite）
    ├── src/                 # 前端代码
    ├── .env.development     # 开发环境配置
    └── package.json
```

---

## 四、环境依赖

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| Docker Desktop | 最新 | 跑 PG / Qdrant / Kafka 容器 |
| Python | 3.12 | 后端运行 |
| Node.js | 18+ | 前端运行 |
| pnpm | 最新 | 前端包管理（也可用 npm） |

---

## 五、后端启动

### 5.1 启动基础组件（Docker）

进入 `memProject` 目录，启动容器：

```bash
cd memProject
docker compose up -d          # 启动 PG + Qdrant + Kafka + Kafka UI
```

启动后端口：

| 服务 | 端口 | 用途 |
|------|------|------|
| PostgreSQL | 5433 | 关系数据 |
| Qdrant | 6333 / 6334 | 向量库（gRPC / HTTP+UI） |
| Kafka | 9092 | 消息队列 |
| Kafka UI | 8080 | Kafka 管理界面 |

> Qdrant 可视化面板：浏览器打开 `http://127.0.0.1:6334/dashboard`

### 5.2 配置环境变量（.env）

编辑 `memProject/.env`，至少确认以下项：

```ini
# 数据库
DB_HOST=localhost
DB_PORT=5433
DB_USER=memuser
DB_PASSWORD=mempassword
DB_NAME=agent_memory

# 向量库
QDRANT_HOST=localhost
QDRANT_PORT=6333

# 大模型（可选兜底：优先在前端「系统设置」配置全局模型/Key，这里作为最后兜底）
DEEPSEEK_API_KEY=sk-xxxx
LLM_MODEL=deepseek-chat

# Embedding（必填）
SILICONFLOW_API_KEY=sk-xxxx

# 鉴权（开发阶段建议 false，跳过 X-API-Key 校验）
AUTH_ENABLED=false
```

### 5.3 安装依赖 + 初始化数据库

```bash
cd memProject
python -m venv .venv                       # 创建虚拟环境
.venv/Scripts/activate                      # Windows 激活（Linux: source .venv/bin/activate）
pip install -r requirements.txt            # 安装依赖

# 应用数据库迁移
.venv/Scripts/python.exe -m alembic upgrade head
```

### 5.4 启动后端

```bash
cd memProject
.venv/Scripts/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

启动成功后：

- API 地址：`http://127.0.0.1:8000`
- 健康检查：`http://127.0.0.1:8000/health`
- Swagger 文档：`http://127.0.0.1:8000/docs`（debug 模式开启）

---

## 六、前端启动

### 6.1 配置 API 地址

编辑 `agent-memory-frontend/.env.development`：

```ini
VITE_API_BASE_URL=http://127.0.0.1:8000
```

### 6.2 安装依赖 + 启动

```bash
cd agent-memory-frontend
pnpm install
pnpm dev
```

启动成功后，浏览器打开 `http://127.0.0.1:3000`。

---

## 七、使用流程（前后端联调主链路）

### 7.1 登录

- 任意用户名 + 密码即可登录（**登录即注册**，用户名不存在会自动创建）
- 开发默认可用：`admin / admin123`

### 7.2 配置全局 LLM 模型/Key（首次必做）

「基础连接设置」页 → 找到「全局默认 LLM 配置」卡片 → 填入模型和 API Key → 保存。

- **这是跑通全项目的关键**：L1 抽取、L2 场景块、L3 画像、会话摘要、去重审计等**所有 LLM 调用**都会回退到这里配置的全局模型/Key
- 已保存的 Key 不会明文回显（加密存储，只显示「已配置」状态）
- 留空则回退到后端 `.env` 的环境变量兜底

### 7.3 创建场景

「场景标识配置」页 → 创建业务场景（如「电商客服」）。场景用于数据隔离，一个业务一个场景。

### 7.4 注册智能体

「智能体注册接入」页 → 选场景 → 填名称 → 注册。

- 注册成功会**弹窗显示 API Key（仅此一次）**，务必保存
- 可选填「LLM 模型」「LLM API Key」给该智能体单独配置模型（不填则用全局默认）

### 7.5 导入记忆

「记忆数据导入」页 → 选智能体 → 上传 JSON/JSONL/CSV 文件。

- 前端内置清洗器，支持 **OpenWebUI / LibreChat / ChatGPT 导出格式**（`history.messages` 对象、`parentId/childrenIds` 树结构）
- 导入后：`L0 原始对话 → Kafka → L1 抽取 → 向量化 → Qdrant`（异步，几秒到几十秒）

### 7.6 多层记忆管理

「多层记忆管理」页，按四层浏览记忆：

| 层级 | 含义 |
|------|------|
| 用户级 | 跨会话长期记忆（会话关闭后升级） |
| 会话级 | 某个会话内的记忆 |
| 任务级 | 某个任务相关的记忆 |
| 智能体级 | 某个智能体名下的记忆（按 agent 筛选） |

### 7.7 多路检索

「多路信号记忆检索」页，输入关键词/问题检索记忆。支持按类型、状态、场景、智能体过滤。

### 7.8 用户画像

「记忆画像」页，查看该用户**所有场景**的画像（分段卡片展示）。

---

## 八、核心概念速览

### 四层记忆

```
L0 原始对话（t_interaction_record）
  → L1 原子记忆（t_memory，抽取出的单条事实/偏好/状态）
  → L2 场景块（t_scene_block，相关记忆聚合成叙事）
  → L3 用户画像（t_persona，长期稳定特征）
```

### 记忆类型（5 类）

| 类型 | 含义 |
|------|------|
| fact | 客观事实、业务对象、约束、截止时间 |
| preference | 用户偏好、习惯、喜好 |
| task_state | 任务进展、状态、待办 |
| process | 工作流、流程、决策、经验 |
| correction | 用户纠正、否定、修订 |

### 会话关闭（记忆升级）

关闭会话时：
- `preference` / `fact` → 升级为**用户级**长期记忆
- `task_state` / `process` / `correction` → 压缩成会话摘要

### LLM 配置优先级

所有调用大模型的地方统一按以下优先级取配置：

```
智能体级配置（注册智能体时填的模型/Key）
  → 全局默认（「系统设置」页配置的全局模型/Key）
  → 后端 .env（DEEPSEEK_API_KEY / LLM_MODEL）
```

- **L1 抽取/生成、会话摘要、长对话压缩**：按所属智能体的 agent 级配置，未配则回退全局
- **L2 场景块、L3 画像、去重审计**：跨智能体（按 `scene + user` 聚合），直接用全局默认
- 任何一层都没配，才回退到 `.env`
