# Offline Debian packages

This directory is populated by `scripts/prepare-offline-debs.sh` on a machine
with network access. Keep the generated `.deb` files beside this README when
building the deploy branch in an isolated network.